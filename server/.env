# Placeholder .env file committed only to satisfy existing tooling references (e.g. docker-compose env_file) during local dev.
# Do NOT add any secrets or overrides here. Instead, create an untracked local env file (for example, .env.local) that is ignored by git.
